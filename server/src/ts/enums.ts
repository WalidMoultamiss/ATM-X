export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}
export enum LicenseType {
  A = "A",
  B = "B",
}

export enum Mimetype {
  JPEG = "image/jpeg",
  PNG = "image/png",
  GIF = "image/gif",
  WEBP = "image/webp",
}
